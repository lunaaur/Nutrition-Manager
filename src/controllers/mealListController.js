const MealList = require('../views/Meal/MealList')
const {Nutrition} = require('../../db/models')
const renderTemplate = require('../lib/renderTemplate')

const renderMealList = async (req, res) => {
  
}


module.exports = {renderMealList}